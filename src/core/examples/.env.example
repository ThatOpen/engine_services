ACCESS_TOKEN=your-access-token-here
API_URL=https://api.thatopen.com
PROJECT_ID=your-project-id-here
# Required only for permissions.ts — obtain from DevTools → Network → any /api request → Authorization header (without "Bearer ")
JWT=
# Required only for execution.ts — obtain from listComponents() or the platform dashboard
COMPONENT_ID=
