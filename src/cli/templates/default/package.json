{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "thatopen serve",
    "build": "vite build",
    "login": "thatopen login --local",
    "publish": "thatopen publish"
  },
  "dependencies": {
    "@thatopen/services": "^{{VERSION}}"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "vite": "^5.2.0"
  }
}
